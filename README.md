# 🤖 Onboarding Bot

Telegram-бот для автоматизации онбординга новых сотрудников в компании.

## 🎯 Возможности

- **Создание карточек новичков** через интерактивный визард (/newhire)
- **Автоматические напоминания** для юриста (за 3 дня) и DevOps (за 1 день)
- **Эскалация** при просрочке задач
- **Отслеживание статусов** через inline-кнопки
- **Права доступа** по ролям (создатель, лидер, юрист, DevOps, админ)

## 📋 Требования

- Python 3.11+
- Telegram Bot Token (от @BotFather)
- ID группы/чата для онбординга

## 🚀 Быстрый старт

### 1. Клонирование

```bash
git clone https://github.com/recruiter-ship-it/OnbordBot.git
cd OnbordBot
```

### 2. Настройка окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=8521520162:AAGpmDfHLPL5hPVvNjsOZe9fwfJvvwctUjg
ONBOARDING_CHAT_ID=-100XXXXXXXXXX
ALLOWED_CREATORS=ВАШ_TELEGRAM_ID
ADMIN_IDS=ВАШ_TELEGRAM_ID
```

### 3. Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### 4. Запуск

```bash
python main.py
```

## 🐳 Docker

### Сборка и запуск

```bash
docker-compose up -d
```

### Просмотр логов

```bash
docker-compose logs -f bot
```

### Остановка

```bash
docker-compose down
```

## ⚙️ Настройка бота в Telegram

### 1. Создание бота

1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен

### 2. Настройка прав в группе

Бот должен быть **администратором** группы с правами:
- ✅ Читать сообщения
- ✅ Отправлять сообщения
- ✅ Редактировать сообщения
- ✅ Удалять сообщения (опционально)

#### Добавление бота в админы:

1. Откройте настройки группы
2. Управление группой → Администраторы → Добавить администратора
3. Выберите бота
4. Убедитесь, что права выше включены

### 3. Получение ID группы

1. Добавьте @userinfobot в группу
2. Отправьте любое сообщение
3. Бот ответит с ID группы (начинается с -100...)
4. Удалите @userinfobot из группы

### 4. Получение своего ID

1. Откройте @userinfobot в личке
2. Отправьте любое сообщение
3. Бот ответит с вашим ID

## 📝 Команды

| Команда | Описание | Права |
|---------|----------|-------|
| `/start` | Приветствие | Все |
| `/help` | Справка | Все |
| `/newhire` | Создать карточку новичка | HR/Рекрутер |
| `/status <id>` | Показать статус карточки | Все |
| `/list open` | Список открытых карточек | Все |
| `/note <id> <текст>` | Добавить заметку | Создатель/Админ |
| `/setdefaults` | Установить юриста/DevOps по умолчанию | Админ |
| `/cancel` | Отменить создание карточки | Создатель |

## 🔘 Inline-кнопки

| Кнопка | Действие | Кто может нажать |
|--------|----------|------------------|
| ✅ Leader acknowledged | Подтвердить готовность | Назначенный лидер |
| 📄 Docs sent | Отметить отправку документов | Назначенный юрист |
| 🔑 Access granted | Отметить выдачу доступов | Назначенный DevOps |
| ✅ Завершить | Завершить карточку | Создатель/Админ |
| 🔄 Переоткрыть | Переоткрыть карточку | Создатель/Админ |
| ℹ️ Статус | Показать историю | Все |
| 📝 Заметка | Добавить заметку | Создатель/Админ |

## 📊 Статусы

### Общий статус карточки

| Статус | Описание |
|--------|----------|
| 🆕 CREATED | Карточка создана |
| 🔄 IN_PROGRESS | В процессе работы |
| ✅ READY_FOR_DAY1 | Готов к первому дню |
| 🏁 COMPLETED | Завершено |

### Статусы ответственных

| Роль | PENDING | Выполнено |
|------|---------|-----------|
| Лидер | ⏳ Ожидает | ✅ ACKNOWLEDGED |
| Юрист | ⏳ Ожидает | ✅ DOCS_SENT |
| DevOps | ⏳ Ожидает | ✅ ACCESS_GRANTED |

## ⏰ Напоминания

- **За 3 дня** до выхода → напоминание юристу
- **За 1 день** до выхода → напоминание DevOps
- **После просрочки** (24ч) → эскалация рекрутеру

## 🗄️ Структура проекта

```
onboarding-bot/
├── bot/
│   ├── __init__.py
│   ├── config.py          # Конфигурация
│   ├── logger.py          # Логирование
│   ├── database/
│   │   ├── __init__.py
│   │   ├── models.py      # Модели БД
│   │   └── session.py     # Сессии БД
│   ├── handlers/
│   │   ├── newhire.py     # Визард создания
│   │   ├── callbacks.py   # Inline-кнопки
│   │   └── commands.py    # Команды
│   ├── keyboards/
│   │   └── inline.py      # Клавиатуры
│   ├── middlewares/
│   │   └── auth.py        # Проверка прав
│   ├── services/
│   │   ├── hire_service.py # Логика найма
│   │   └── scheduler.py    # Напоминания
│   ├── states/
│   │   └── newhire.py     # FSM состояния
│   └── utils/
│       └── date_utils.py  # Утилиты
├── main.py                # Точка входа
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

## 🔧 Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `BOT_TOKEN` | Токен бота от @BotFather | ✅ |
| `ONBOARDING_CHAT_ID` | ID группы для карточек | ✅ |
| `ALLOWED_CREATORS` | ID создателей (через запятую) | ✅ |
| `ADMIN_IDS` | ID админов (через запятую) | ✅ |
| `DB_URL` | URL базы данных | ❌ (SQLite по умолчанию) |
| `DEFAULT_LEGAL_USERNAME` | Username юриста по умолчанию | ❌ |
| `DEFAULT_DEVOPS_USERNAME` | Username DevOps по умолчанию | ❌ |
| `TIMEZONE` | Часовой пояс | ❌ (Europe/London) |
| `LEGAL_REMINDER_DAYS` | Дней до напоминания юристу | ❌ (3) |
| `DEVOPS_REMINDER_DAYS` | Дней до напоминания DevOps | ❌ (1) |
| `ESCALATION_HOURS` | Часов до эскалации | ❌ (24) |
| `SCHEDULER_INTERVAL_MINUTES` | Интервал проверки | ❌ (30) |

## 📈 Сценарии использования

### Happy Path

1. Рекрутер отправляет `/newhire` в личке боту
2. Бот задаёт вопросы о новом сотруднике
3. После подтверждения бот публикует карточку в чате группы
4. Лидер, юрист и DevOps получают уведомления
5. Каждый отмечает свой статус кнопкой
6. Когда все статусы выполнены → READY_FOR_DAY1
7. Рекрутер завершает карточку → COMPLETED

### Edge Cases

- **Пользователь без прав пытается создать карточку**: Бот отклоняет с сообщением о недоступности
- **Не тот пользователь нажимает кнопку**: Бот показывает алерт о недостатке прав
- **Карточка просрочена**: Автоматическая эскалация рекрутеру
- **Юрист/DevOps не в группе**: Упоминание в чате + попытка личного сообщения

## 🔒 Безопасность

- Токен бота хранится в `.env` (не коммитьте!)
- Проверка прав на каждое действие
- Валидация всех вводов пользователя
- SQL-инъекции защищены через SQLAlchemy

## 📜 Лицензия

MIT License
