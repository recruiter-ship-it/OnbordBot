# Onboarding Bot

Telegram-бот для автоматизации процесса онбординга новых сотрудников в компании.

## Возможности

- 🎯 Создание карточек новичков через wizard (/newhire)
- 📋 Отслеживание статусов: Leader, Legal, DevOps
- 🔔 Автоматические напоминания
- 🚨 Эскалация при просрочках
- 📊 История изменений
- 🔐 Контроль доступа по ролям

## Требования

- Python 3.11+
- Telegram Bot Token (от @BotFather)
- ID чата/группы для онбординга

## Быстрый старт

### 1. Клонирование

```bash
git clone https://github.com/recruiter-ship-it/OnbordBot.git
cd OnbordBot
```

### 2. Настройка окружения

```bash
# Копируем пример конфигурации
cp .env.example .env

# Редактируем .env
nano .env
```

### 3. Получение данных

**Bot Token:**
1. Откройте @BotFather в Telegram
2. Отправьте /newbot
3. Следуйте инструкциям
4. Скопируйте токен в BOT_TOKEN

**Chat ID:**
1. Добавьте @userinfobot в ваш чат
2. Он покажет ID чата (для групп начинается с -100)
3. Скопируйте в ONBOARDING_CHAT_ID

**Ваш User ID:**
1. Напишите @userinfobot в личку
2. Скопируйте ваш ID в ALLOWED_CREATORS и ADMIN_IDS

### 4. Установка и запуск

```bash
# Создаём виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate на Windows

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем
python main.py
```

## Docker

```bash
# Сборка
docker-compose build

# Запуск
docker-compose up -d

# Логи
docker-compose logs -f

# Остановка
docker-compose down
```

## Настройка прав бота в группе

### Обязательные права:

1. **Добавьте бота в группу**
   - Как обычный участник или с правами администратора

2. **Права администратора (рекомендуется):**
   - ✅ Управление сообщениями (редактирование)
   - ✅ Удаление сообщений
   - ✅ Закрепление сообщений (опционально)

3. **Групповые права:**
   - Group Anonymous Bot должен быть админом (для работы с @username)

### Включение режима Groups:

```bash
# Через BotFather:
/setprivacy -> выберите бота -> Disable
```

Это позволит боту видеть все сообщения в группе.

## Команды бота

| Команда | Описание | Доступ |
|---------|----------|--------|
| `/start` | Начать работу | Все |
| `/help` | Справка | Все |
| `/newhire` | Создать карточку новичка | Рекрутеры |
| `/status <id>` | Показать статус | Все |
| `/list open` | Список открытых | Рекрутеры |
| `/setdefaults` | Настроить умолчания | Админы |
| `/cancel` | Отменить создание | Все |

## Workflow

### Создание карточки (Happy Path)

```
1. Рекрутер: /newhire в личке с ботом
2. Бот: Запрашивает ФИО
3. Рекрутер: Иван Иванов
4. Бот: Запрашивает дату выхода
5. Рекрутер: 2024-02-15
6. Бот: Запрашивает роль
7. Рекрутер: Backend Developer
8. Бот: Запрашивает @username лидера
9. Рекрутер: @team_lead
10. Бот: Запрашивает @username юриста (по умолчанию)
11. Рекрутер: Enter (использует умолчание)
12. Бот: Запрашивает @username DevOps
13. Рекрутер: @devops_team
14. Бот: Запрашивает email для документов
15. Рекрутер: ivan@company.com
16. Бот: Показывает чеклист доступов (кнопки)
17. Рекрутер: Выбирает Email, GitHub, VPN, Slack
18. Бот: Запрашивает примечания
19. Рекрутер: Начинает 15 февраля
20. Бот: Показывает превью и просит подтвердить
21. Рекрутер: ✅ Подтвердить
22. Бот: Создаёт карточку, публикует в чат, уведомляет ответственных
```

### Обновление статусов

```
1. Leader видит карточку в чате
2. Нажимает "✅ Leader acknowledged"
3. Бот проверяет права (по @username)
4. Бот обновляет статус и редактирует сообщение
5. История записывается в БД
```

### Напоминания

```
- За 3 дня до выхода: напоминание Legal (если DOCS_SENT=false)
- За 1 день до выхода: напоминание DevOps (если ACCESS_GRANTED=false)
- После просрочки >24ч: эскалация рекрутеру
```

## Структура проекта

```
onboarding-bot/
├── bot/
│   ├── database/
│   │   ├── models.py      # SQLAlchemy модели
│   │   └── session.py     # Сессии БД
│   ├── handlers/
│   │   ├── newhire.py     # Wizard создания
│   │   ├── callbacks.py   # Inline кнопки
│   │   └── commands.py    # Команды
│   ├── keyboards/
│   │   └── inline.py      # Клавиатуры
│   ├── middlewares/
│   │   └── access.py      # Контроль доступа
│   ├── scheduler/
│   │   └── reminders.py   # Напоминания
│   ├── services/
│   │   └── hire_service.py # Бизнес-логика
│   ├── states/
│   │   └── newhire.py     # FSM состояния
│   ├── utils/
│   │   └── formatting.py  # Форматирование
│   ├── config.py          # Конфигурация
│   └── logger.py          # Логирование
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

## База данных

### Таблицы

**hires:**
- hire_id (PK) — уникальный ID (4 символа)
- full_name — ФИО
- start_date — дата выхода
- role — роль/позиция
- leader_username, legal_username, devops_username
- leader_id, legal_id, devops_id (Telegram ID, опционально)
- docs_email — email для документов
- access_checklist (JSON) — выбранные доступы
- notes — примечания
- status — общий статус
- leader_status, legal_status, devops_status
- chat_id, message_id — для редактирования сообщения
- creator_id — создатель
- created_at, updated_at

**status_history:**
- id (PK)
- hire_id (FK)
- actor_id, actor_username
- action, details
- ts — timestamp

**default_settings:**
- id (PK)
- key, value
- updated_at

## Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| BOT_TOKEN | Токен бота от BotFather | ✅ |
| DB_URL | URL базы данных | ❌ (SQLite по умолчанию) |
| ONBOARDING_CHAT_ID | ID чата для карточек | ✅ |
| ALLOWED_CREATORS | ID создателей (через запятую) | ✅ |
| ADMIN_IDS | ID админов (через запятую) | ❌ |
| DEFAULT_LEGAL_USERNAME | Юрист по умолчанию | ❌ |
| DEFAULT_DEVOPS_USERNAME | DevOps по умолчанию | ❌ |
| TIMEZONE | Таймзона (Europe/London) | ❌ |
| LEGAL_REMINDER_DAYS | Дней до напоминания Legal | ❌ (3) |
| DEVOPS_REMINDER_DAYS | Дней до напоминания DevOps | ❌ (1) |
| ESCALATION_HOURS | Часов до эскалации | ❌ (24) |
| SCHEDULER_INTERVAL_MINUTES | Интервал проверки | ❌ (30) |

## Безопасность

- ⚠️ **Никогда не коммитьте .env файл!**
- Используйте секреты в CI/CD
- Ограничьте список ALLOWED_CREATORS
- Регулярно проверяйте права бота

## Расширение

### Добавление PostgreSQL

```yaml
# docker-compose.yml
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: onboarding
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: onboarding
    volumes:
      - postgres_data:/var/lib/postgresql/data
```

```bash
# .env
DB_URL=postgresql+asyncpg://onboarding:${DB_PASSWORD}@postgres:5432/onboarding
```

### Добавление новых статусов

1. Отредактируйте `bot/database/models.py`
2. Добавьте новые enum значения
3. Обновите `bot/keyboards/inline.py`
4. Добавьте обработчик в `bot/handlers/callbacks.py`

## Лицензия

MIT

## Автор

Onboarding Bot Team
